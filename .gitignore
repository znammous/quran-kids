# ملفات الخطط (PDF) — كبيرة الحجم، تُوزَّع خارج المستودع
*.pdf

# لقطاتُ الفحص وإعداداتُ الأداة — محلّيّةٌ لا تُرفع
.claude/
_probe.html

# أدواتُ المحرّر — لا تُرفع: ما دخل المستودعَ صار متاحاً للعموم على
# GitHub Pages، وهذه للمحرّر وحدَه على جهازه
tools/

# صورُ القصص الأصليّة قبل المعالجة — كبيرة، تبقى على الجهاز
Raw Images/

# التطبيقُ الأصليّ: ما يُبنى ويُنزَّل لا يُرفع
node_modules/
www/
*.keystore
*.jks
keystore.properties
